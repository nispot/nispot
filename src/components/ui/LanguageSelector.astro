---
import { getLangFromUrl } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const otherLang = lang === 'en' ? 'es' : 'en';
---

<a
  href={`/${otherLang}/`}
  class='p-2 flex items-center font-medium hover:text-brand transition-colors'
>
  {otherLang.toUpperCase()}
</a>
